import {StyleSheet} from "react-native"

export const colors={
    black: '#000',
    white: '#fff',
    primary: '#FFDF2B',
    secondary: '#00198E'
}

export const styleContainer = StyleSheet.create({
    container:{
        flex: 1
    }
}
)